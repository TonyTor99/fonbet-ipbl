"""Конфигурация сборщика рынков шорт-хоккея (отдельный процесс).

Мониторит ВСЕ лиги шорт-хоккея Fonbet (название спорта начинается с
"Шорт-хоккей.") — 3HL North/South, MNHL 2x2, MNHL B 2x2, RHL Center/West/East
и любые новые. Каждую игровую минуту пишет крайнюю линию каждого рынка матча
в отдельную БД; отдельно — одну строку ДО начала матча ("Не начался"); на
финале проставляет результат каждого исхода (зашла/не зашла).

Сетевые настройки (серверы, заголовки, scopeMarket, интервал) переиспользуются
из основного config.py, чтобы не дублировать.
"""

# Признак лиги шорт-хоккея: sport.name.upper() начинается с этой строки.
LEAGUE_PREFIX = "ШОРТ-ХОККЕЙ"

# Отдельный файл БД рядом с проектом.
SH_COLLECTOR_DB = "shorthockey_markets.db"

# --- factorId рынков матча (root customFactors) ---------------------------
# Проверено на живом API шорт-хоккея (июль 2026).

WIN1_FID = 921          # Исход: Победа 1 (П1)
DRAW_FID = 922          # Исход: Ничья (X)
WIN2_FID = 923          # Исход: Победа 2 (П2)

DC_1X_FID = 924         # Двойной шанс 1X (П1 или ничья)
DC_X2_FID = 925         # Двойной шанс X2 (ничья или П2)
DC_12_FID = 1571        # Двойной шанс 12 (П1 или П2, без ничьи)

# Фора: крайняя = самая отрицательная линия К1; парный К2 на -line.
FORA1_FIDS = {910, 927, 989, 1569, 1672}     # Фора 1-й команды (обычно отриц.)
FORA2_FIDS = {912, 928, 991, 1572, 1675}     # Фора 2-й команды (обычно полож.)

# Тотал / инд.тоталы: крайняя = минимальная линия, где есть обе стороны.
TOTAL_B_FIDS = {930, 1696, 1727, 1730, 1733, 1736}   # Тотал Больше
TOTAL_M_FIDS = {931, 1697, 1728, 1731, 1734, 1737}   # Тотал Меньше
IT1_B_FIDS = {974, 1809, 1812}                        # Инд. тотал К1 Больше
IT1_M_FIDS = {976, 1810, 1813}                        # Инд. тотал К1 Меньше
IT2_B_FIDS = {978, 1854, 1873}                        # Инд. тотал К2 Больше
IT2_M_FIDS = {980, 1871, 1874}                        # Инд. тотал К2 Меньше

# Метка предматчевой строки (game_minute-сентинел, чтобы UNIQUE держал её одну).
PREMATCH_MINUTE = -1
PREMATCH_COMMENT = "Не начался"
