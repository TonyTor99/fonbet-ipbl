"""Конфигурация сборщика рынков КИБЕРФУТБОЛА FC 26 (отдельный процесс).

Мониторит ВСЕ лиги киберфутбола FC 26 Fonbet (название спорта начинается с
"FC 26") — United Esports Leagues, H2H LIGA-1/2/3, ESportsBattle La Liga /
Champions League B и любые новые. Матч длится ~3-6 реальных минут, но игровой
таймер идёт 0 → 90 игровых минут (timerSeconds 0 → 5400).

Пишем одну строку ДО начала матча ("Не начался") и далее по одной строке на
каждой 5-й ИГРОВОЙ минуте (5, 10, 15, ..., 90). На финале — дорасчёт результата
каждого исхода по итоговому счёту матча.

Сетевые настройки (серверы, заголовки, scopeMarket) переиспользуются из
основного config.py. Интервал поллинга свой — матч быстрый.
"""

# Признак лиги киберфутбола: sport.name.upper() начинается с этой строки.
LEAGUE_PREFIX = "FC 26"

# Форматы с РЕАЛЬНЫМ временем (не 0-90), которые нельзя собирать по игровым
# минутам. Volta (3x3, "2x3 мин") показывает реальное время матча ~6 мин, а не
# игровое 0-90 — исключаем по подстроке в названии лиги.
EXCLUDE_SUBSTRINGS = ("VOLTA",)

# Отдельный файл БД рядом с проектом.
CYBER_COLLECTOR_DB = "cyberfootball_markets.db"

# Свой интервал поллинга: матч ~3-6 мин на 90 игровых, чтобы не пропускать
# 5-минутные отметки, опрашиваем чаще основного сборщика.
CYBER_POLL_INTERVAL = 6

# Игровое время киберфутбола (секунды таймера Fonbet).
HALFTIME_TS = 2700          # 45:00 — граница таймов
FULLTIME_TS = 5400          # 90:00
STEP_MINUTES = 5            # шаг сбора по игровому времени

# --- factorId рынков матча (root customFactors) ---------------------------
# Проверено на живом API FC 26 (сентябрь 2026).

WIN1_FID = 921          # Исход: Победа 1 (П1)
DRAW_FID = 922          # Исход: Ничья (X)
WIN2_FID = 923          # Исход: Победа 2 (П2)

DC_1X_FID = 924         # Двойной шанс 1X (П1 или ничья)
DC_X2_FID = 925         # Двойной шанс X2 (ничья или П2)
DC_12_FID = 1571        # Двойной шанс 12 (П1 или П2, без ничьи)

# Фора: только ОСНОВНАЯ линия (крайняя = самая отрицательная линия К1; парный К2 на -line).
FORA1_FIDS = {910, 927, 989, 1569, 1672}     # Фора 1-й команды (обычно отриц.)
FORA2_FIDS = {912, 928, 991, 1572, 1675}     # Фора 2-й команды (обычно полож.)

# Тоталы матча и инд. тоталы: собираем ВСЕ линии (в дочернюю таблицу).
TOTAL_B_FIDS = {930, 1696, 1727, 1730, 1733, 1736}   # Тотал Больше
TOTAL_M_FIDS = {931, 1697, 1728, 1731, 1734, 1737}   # Тотал Меньше
IT1_B_FIDS = {974, 1809, 1812}                        # Инд. тотал К1 Больше
IT1_M_FIDS = {976, 1810, 1813}                        # Инд. тотал К1 Меньше
IT2_B_FIDS = {978, 1854, 1873}                        # Инд. тотал К2 Больше
IT2_M_FIDS = {980, 1871, 1874}                        # Инд. тотал К2 Меньше

# Обе забьют (ДА/НЕТ).
BTTS_YES_FID = 4241     # Обе забьют — Да
BTTS_NO_FID = 4242      # Обе забьют — Нет

# Метка предматчевой строки (game_minute-сентинел, чтобы UNIQUE держал её одну).
PREMATCH_MINUTE = -1
PREMATCH_COMMENTS = {"не начался", "матч не начался"}
